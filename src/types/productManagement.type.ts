export type TProduct = {
  _id: string;
  name: string;
  brand: string;
  price: number;
  model: string;
  description: string;
  quantity: number;
  stock: boolean;
  createdAt: string;
  updatedAt: string;
};
